# Architecture

The plugin uses a small PHP core for registering post types and admin pages. React components are compiled via `@wordpress/scripts` and rendered in the WordPress admin with TailwindCSS styles. The front-end form is built with PHP and TailwindCSS.
