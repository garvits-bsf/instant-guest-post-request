# Architecture

The plugin uses a small PHP core for registering post types and admin pages. React components are compiled via `@wordpress/scripts` and rendered only in the WordPress admin using Force UI components. The front-end form is built with PHP and TailwindCSS.
